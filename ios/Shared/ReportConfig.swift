import Foundation

enum ReportConfig {
    // Production JSON served by GitHub Pages.
    static let reportURL = URL(string: "https://eduardeden.github.io/loto-report/report.json")!
    static let appGroupIdentifier = "group.com.eduardcramaroc.LotoReport"
}
