import Foundation

enum ReportConfig {
    // Replace with your real GitHub Pages URL after repository setup.
    static let reportURL = URL(string: "https://example.github.io/loto-report/report.json")!
    static let appGroupIdentifier = "group.com.eduardcramaroc.LotoReport"
}
